index.html
├── <head>            → 引入 Phaser 游戏框架（CDN）
├── <style>           → 页面外观（背景色、居中、边框发光）
└── <script>          → 游戏核心逻辑
    ├── 游戏配置       → 网格大小、移动速度、颜色
    ├── SnakeScene     → 游戏场景（核心）
    │   ├── create()   → 初始化：蛇、食物、分数、键盘
    │   ├── update()   → 每帧运行：检测输入、移动蛇
    │   ├── handleInput()  → 处理方向键/WASD
    │   ├── moveSnake()    → 蛇的移动逻辑
    │   ├── spawnFood()    → 随机生成食物
    │   ├── draw()         → 每帧绘制蛇和食物
    │   └── endGame()      → 游戏结束
    └── 启动游戏       → 创建 Phaser 实例





开始 → 蛇向右移动 → 等待键盘输入
   ↓
每130ms移动一次 → 检查撞墙/撞自己？
   ↓ 否              ↓ 是
蛇头往前走       游戏结束，显示分数
   ↓
碰到食物？ → 是 → 加分，蛇变长，生成新食物
   ↓ 否
删掉尾巴（蛇长度不变）
   ↓
重绘画面 → 回到"等待键盘输入"
